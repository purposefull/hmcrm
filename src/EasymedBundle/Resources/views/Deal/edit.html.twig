{% extends '::dashboard.html.twig' %}

{% block title %} {{ 'deal.edit'|trans }} {% endblock %}

{% block body -%}
    {{ form_start(edit_form) }}
        <div class="form-actions">
            <div class="col-sm-12">
                {{ form_widget(edit_form.submit, { 'attr': {'class': 'btn btn-primary'}}) }}
                <a class="btn btn-default" href="{{ path('deal') }}">
                    {{ 'backToTheList'|trans }}
                </a>
            </div>
        </div>

        {{ form_rest(edit_form) }}
    {{ form_end(edit_form) }}
{% endblock %}
